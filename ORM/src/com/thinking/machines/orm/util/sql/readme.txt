ForeSQL.java -

It is person which is responsible to use JDBC and do all operations in DB

FireSQL provides following services -
- isExists -
	It will fire sql & check existence return true or false

- insert
	It will fire insert statement and add data into table,
	and if autoincrement field return it will returns too
